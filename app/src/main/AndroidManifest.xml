<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.intrasonics.mobile.numericanalyser">

    <application
        android:name=".base.NumericAnalyserApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".feature.welcome.WelcomeActivity"
            android:label="@string/common_activity_title">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".feature.about.AboutActivity"
            android:label="@string/common_activity_title"/>
    </application>
</manifest>
